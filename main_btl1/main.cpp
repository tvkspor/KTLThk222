#include "knight.h"

int main(int argc, char** argv) {
    string s1 = "testcase_input";
    string file_input(s1);
    int HP, level, remedy, maidenkiss, phoenixdown, rescue;
    adventureToKoopa(file_input, HP, level, remedy, maidenkiss, phoenixdown, rescue);
    return 0;
}